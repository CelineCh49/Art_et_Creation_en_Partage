{% extends 'base.html.twig' %}

{% block title %}Modifier une fiche Artiste
{% endblock %}

{% block body %}
	<h1>Modifier une fiche Artiste</h1>

	{{ include('artist/_form.html.twig', {'button_label': 'Modifier'}) }}

	
		<div>Toutes mes photos:</div>

		{% for image in artist.artistImages %}
			<div>
				<img src="{{asset('assets/uploads/artist_img/'~ image.fileName)}}" alt="Image {{image.fileName}}" width="150">
				{{ include('artist/_delete_image_form.html.twig') }}
				{# <a href="{{path('app_artist_delete_image', {'id':image.id}) }}"
				            data-delete data-token = "{{ csrf_token('delete'~ image.id) }}">Supprimer</a> #}
			</div>
		{% endfor %}

		<a href="{{ path('app_artist_show', {'id': artist.id}) }}">Retour sur ma fiche</a>


		<a href="{{ path('app_artist_index') }}">Voir tous les artistes</a>

		{{ include('artist/_delete_form.html.twig') }}
	{% endblock %}

	{# {% block javascripts %}
	    <script src="{{asset('assets/js/images.js') }}"></script>
	{% endblock %} #}
