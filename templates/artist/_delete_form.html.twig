<form method="post" action="{{ path('app_artist_delete', {'id': artist.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet artiste ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist.id) }}">
    <button class="btn">Supprimer l'artiste</button>
</form>
