<nav>
    <div>
        <a href="{{path('app_main')}}">Logo Art et création</a>
        <a href="{{path('app_main')}}">Accueil</a>
        <a href="{{path('app_about_us')}}">A propos</a>
        <a href="{{path('app_event_index')}}">Evénements</a>
        <a href="{{path('app_artist_index')}}">Artistes</a>
        {# <a href="{{path('app_category_index')}}">Catégories</a> #}
        <a href="{{path('app_contact')}}">Contact</a>
        <a href="#">Facebook</a>
        <a href="#">Instagram</a>
        {% if app.user %}
            <a href="{{ path('my_profile')}}" class=" text-gray-800 rounded-md px-3 py-2 text-sm font-medium">Mon profil</a>
            {# <a href="{{ path('app_artist_edit', {'id': app.user.artist.id} ) }}" class=" text-gray-800 rounded-md px-3 py-2 text-sm font-medium">Ma fiche artiste</a> #}
            {{ app.user.userIdentifier }}
            <a href="{{ path('app_logout')}}" class=" text-gray-800 rounded-md px-3 py-2 text-sm font-medium">Déconnexion</a>
		{% else %}
            <a href="{{ path('app_login')}}" class="bg-secondary-400 hover:bg-primary-600 text-gray-800 rounded-md px-3 py-2 text-sm font-medium">Se connecter</a>
		{% endif %}
        {# <a href="{{path('app_login')}}">Se connecter</a> #}
    </div>
</nav>

			

            