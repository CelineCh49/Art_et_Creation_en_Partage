<form method="post" action="{{ path('app_artist_image_delete', {'id': artist_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist_image.id) }}">
    <button class="btn">Delete</button>
</form>
