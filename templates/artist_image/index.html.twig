{% extends 'base.html.twig' %}

{% block title %}ArtistImage index{% endblock %}

{% block body %}
    <h1>ArtistImage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>FileName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artist_image in artist_images %}
            <tr>
                <td>{{ artist_image.id }}</td>
                <td>{{ artist_image.fileName }}</td>
                {# <td>{{ image.id }}</td>
                <td>{{ image.fileName }}</td>
                <td>{{ image.artist.artistName }}</td> #}
                <td>
                    <a href="{{ path('app_artist_image_show', {'id': artist_image.id}) }}">show</a>
                    <a href="{{ path('app_artist_image_edit', {'id': artist_image.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_artist_image_new') }}">Create new</a>
{% endblock %}
