{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="flex justify-center my-2 rounded-md bg-teal-900 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-teal-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-600 mt-4">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}

{# <form class="space-y-6" action="#" method="POST">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<div>
					<button type="submit" class="flex justify-center my-2 rounded-md bg-teal-900 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-teal-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-600 mt-4">Modifier</button>
				</div>
				{{ form_end(form) }}
			</form> #}