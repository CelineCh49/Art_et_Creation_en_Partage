{% extends 'base.html.twig' %}

{% block title %}Créer un événement{% endblock %}

{% block body %}
<div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-8 lg:max-w-7xl lg:px-8">
		<div class="flex items-center justify-between pb-10">
			<h2 class="text-2xl font-bold tracking-tight text-gray-900 ">
				Créer un événement</h2>
		</div>
		<div>
			{{ include('event/_form.html.twig') }}
		</div>
        <div class="pt-6">
			<a href="{{ path('app_event_index') }}" class="underline hover:text-teal-800">Retour à la liste</a>
		</div>
</div>
{% endblock %}
